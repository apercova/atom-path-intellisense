{
  "name": "atom-path-intellisense",
  "main": "./lib/atom-path-intellisense",
  "version": "1.1.1",
  "description": "Path intellisense support for atom",
  "keywords": [
    "path",
    "paths",
    "autocomplete",
    "suggestion",
    "suggestions",
    "intellisense"
  ],
  "activationCommands": {},
  "repository": "https://github.com/apercova/atom-path-intellisense",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "scripts": {
    "docs": "rm -Rf docs && mkdir doc && ./node_modules/.bin/jsdoc lib/**/*.js --destination docs"
  },
  "dependencies": {
    "atom-selectors-plus": "^0.1.1",
    "winston": "^3.2.1"
  },
  "providedServices": {
    "autocomplete.provider": {
      "versions": {
        "4.0.0": "getProvider"
      }
    }
  },
  "devDependencies": {
    "eslint": "^6.5.1",
    "jsdoc": "~3.6.3",
    "prettier": "^1.18.2"
  }
}
